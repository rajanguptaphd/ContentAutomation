generator:
  backend: ollama          # hf | ollama
  model_name: qwen2.5:1.5b-instruct
  device: cpu              # hf only
  max_new_tokens: 80

validator:
  backend: ollama
  model_name: qwen2.5:1.5b-instruct
  device: cpu
  max_new_tokens: 40
